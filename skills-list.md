# skills-list — 列出所有已安装的 Skill

## 概述

`skills-list` **列出系统当前所有已安装的 skill**，按分类分组展示，方便用户了解可用功能。

## 数据来源

调用内置 `skills_list` 工具获取所有已注册 skill（返回 name、description、category、count）。 然后将 description 翻译为中文返回。
